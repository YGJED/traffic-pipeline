# ── Core pipeline libraries ───────────────────────────────────────────────
pyspark>=3.5.1          # distributed data processing engine
kafka-python>=2.0.2     # Kafka producer/consumer in Python
boto3>=1.24.28          # AWS S3 access
pyarrow>=15.0.0         # read/write Parquet files
python-dotenv>=1.0.0    # load .env for local configuration (e.g. scripts using AWS)

# ── API and dashboard ─────────────────────────────────────────────────────
fastapi>=0.110.0        # REST API server
uvicorn>=0.29.0         # runs FastAPI
streamlit>=1.33.0       # dashboard UI

# ── Data handling ─────────────────────────────────────────────────────────
pandas>=2.2.1           # used by FastAPI to read Parquet from S3
s3fs>=2023.3.0          # lets pandas read directly from S3 paths

# ── Mapping (for Streamlit map view) ─────────────────────────────────────
folium>=0.16.0          # interactive maps
streamlit-folium>=0.20.0  # embed Folium maps in Streamlit
